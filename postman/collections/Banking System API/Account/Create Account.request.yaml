$kind: http-request
name: Create Account
method: POST
url: '{{baseUrl}}/api/account'
order: 1000
description: Create a new bank account for the authenticated user. Requires a valid JWT token in the Authorization header.
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
